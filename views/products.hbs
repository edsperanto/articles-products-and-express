{{#if displayAll}}
	<header>
		<h1>All Products</h1>
	</header>
	<div class='item-container'>
		{{#each product}}
			{{> product}}
		{{/each}}
	</div>
{{else}}
	<header>
		<h1>Product</h1>
	</header>
	<div class='item-container'>
		{{#with product}}
			{{> product}}
		{{/with}}
	</div>
{{/if}}
